pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'mvn build'
            }
        }
         stage('Test') {
            steps {
                echo 'Testing...'
                sh 'mvn test'
            }
        }
        stage('Build Docker Image') {
            steps {
                echo 'Building Docker image...'
                sh 'mvn docker'
            }
        }
        stage('Run Docker Image') {
            steps {
                echo 'Running Docker image...'
                sh 'mvn dockerRun'
            }
        }
    }
}